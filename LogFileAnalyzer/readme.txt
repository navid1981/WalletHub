1-db.properties and access.log should be in the same folder of executable jar(parser.jar).

2-To set application with your database, please set url, username and password of your database in db.properties.

Note:This application create java schema and all required tables(LogTable, BlockIp) in db and it is included mySQL and Oracle12 driver.

Note:The applicatin was tested by mySQL database and it works as desired.
